If the apt-installed packages are not visible in venv:
```
export PYTHONPATH=/usr/lib/python3/dist-packages:$PYTHONPATH
```

Activate venv 
```
source ~/python_venvs/labdata_streamer/bin/activate
```
